# A JSON context file merges with a YAML one exactly as another YAML file
# would, and keeps JSON-only spelling such as the escaped slash.
name: $$.app.name
replicas: $$.app.replicas
mount: $$.paths.data
limits: $$.limits
tags: $$.tags
